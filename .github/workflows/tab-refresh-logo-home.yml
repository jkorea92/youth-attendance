name: Tab refresh and logo home
on:
  push:
    branches: [main]
    paths:
      - 'scripts/tab_refresh_and_logo_home.py'
      - '.github/workflows/tab-refresh-logo-home.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/tab_refresh_and_logo_home.py
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo 'No changes'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html app-v4.html
          git commit -m 'Refresh previous tab and make logo open dashboard'
          git push
